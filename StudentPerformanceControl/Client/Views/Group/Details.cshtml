@model Entity.Models.Dtos.GroupDto
@{
    ViewData["Title"] = "Details";
    var studentIndex = 1;
    var subjectIndex = 1;
}

<h1 class="align-content-center m-5">@Model.Title group</h1>


<div class="m-5">
     <h3 class="m-5"> Subject </h3>
        <table class="table table-striped">
            <thead class="thead-dark">
            <tr>
                <th>Number</th>
                <th>Title</th>
            </tr>
            </thead>
            <tbody>
                @foreach (var subject in Model.Subjects)
                {
                    <tr>
                        <td>@subjectIndex</td>
                        <td>@subject.SubjectName</td>
                    </tr>
                    subjectIndex++;
                }
            </tbody>
        </table>
</div>

<div class="m-5">
    <h3 class="m-5"> Students </h3>
    <table class="table table-striped">
        <thead class="thead-dark">
        <tr>
            <th>Number</th>
            <th>SecondName</th>
            <th>Name</th>
        </tr>
        </thead>
        <tbody>
            @foreach (var student in Model.Students)
            {
                <tr>
                    <td>@studentIndex</td>
                    <td>@student.SecondName</td>
                    <td>@student.Name</td>
                </tr>
                studentIndex++;
            }
        </tbody>
    </table>
</div>


